/**
 * TODO (task D2): build the Provider Market Share page.
 *
 * Requirements are in CASE_STUDY.md -> "D2". In short:
 *   - geography dropdown (reuse <GeographySelect />) + month picker (default 2026-08)
 *   - table of providers from GET /api/provider-market-share/
 *   - click a column header to sort by it
 *   - a visual cue for share (e.g. an inline bar)
 *   - loading, empty and error states
 *   - a footer / summary that lets a reader sanity-check the numbers
 */
export default function MarketShare() {
  return (
    <section>
      <div className="page-head">
        <h1>Provider Market Share</h1>
      </div>
      <p className="muted">Not built yet.</p>
    </section>
  );
}
